# Snippet benches

This directory contains small benches that were done during the implementation of Vulkayes to decide on coding strategies. Needs `rustc`, but `cargo` is recommended. Run by issuing `cargo bench`.