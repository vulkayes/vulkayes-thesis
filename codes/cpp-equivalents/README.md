# Cpp equivalents

This directory contains code samples to compare properties of Rust and C++ implementation of the same concepts. Shell commands to compile are present in `.vscode/tasks.json` file. `clang` and `rustc` are required.
